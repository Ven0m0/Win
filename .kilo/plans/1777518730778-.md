# Repository Cleanup & Consolidation Plan

## Objective
Clean, consolidate, and normalize the Ven0m0/Win repository while preserving all external behavior.

---

## Phase 1: Remove Dead Files and Orphans

| File | Reason |
|------|--------|
| `Scripts/network-tune.ps1` | 575B stub; unreferenced anywhere |
| `Scripts/manual-win-updt.ps1` | Interactive-only; unreferenced; duplicates system-update logic |
| `Scripts/Updates.cmd` | Unreferenced by any script/manifest/CI |
| `Scripts/administrator.reg` | Orphan; zero references |
| `Scripts/reg/administrator.reg` | Orphan; zero references |
| `Scripts/reg/Menu.reg` | Orphan; zero references |
| `Scripts/PowerShell-Context-Menus.reg` | Orphan; zero references |
| `.github/scripts/lint_format_ps.py` | Orphan; no callers |
| `.github/scripts/autoformat.py` | Orphan; no callers |
| `.github/scripts/add_requires.py` | Orphan; no callers |
| `.github/scripts/Format-PowerShell.ps1` | Superseded by inline CI job |
| `.github/scripts/Lint-PowerShell.ps1` | Superseded by inline CI job |
| `.github/scripts/Test-PowerShell.ps1` | Superseded by inline CI job |

**Action:** Delete files. Update `lint-format-test.yml` path triggers to remove references to deleted `.github/scripts/*.ps1` files.

---

## Phase 2: Consolidate Duplicate Logic into Common.ps1

### 2a. VDF helpers (`steam.ps1` ↔ `start-arc-raiders.ps1`)
- **Current:** `vdf_mkdir` defined identically in both files.
- **Action:** Move `function vdf_mkdir` to `Common.ps1`.
- **Call sites:** Remove definitions from `steam.ps1` and `start-arc-raiders.ps1`; ensure both dot-source `Common.ps1`.

### 2b. `sc-nonew` helper
- **Current:** Defined in `steam.ps1` and `start-arc-raiders.ps1`.
- **Action:** Move to `Common.ps1`; update call sites.

### 2c. `Ensure-Directory` helper
- **Current:** Repeated `if (-not (Test-Path $path)) { New-Item -ItemType Directory -Path $path -Force | Out-Null }` across 5+ scripts.
- **Action:** Add `function Ensure-Directory` to `Common.ps1`; replace inline blocks.

### 2d. Elevation standardization
- **Current:** 8 scripts contain copy-paste `IsInRole` + `Start-Process -Verb RunAs` blocks.
- **Action:** Replace inline elevation with `Request-AdminElevation` from `Common.ps1` where appropriate. Scripts affected:
  - `Scripts/system-update.ps1`
  - `Scripts/Setup-Win11.ps1`
  - `Scripts/Setup-Dotfiles.ps1`
  - `Scripts/Install-Packages.ps1`
  - `Scripts/arc-raiders/game-boost.ps1`
  - `Scripts/arc-raiders/ARCRaidersUtility.ps1`
  - `Scripts/SkipVideosMod.ps1`
  - `Scripts/manual-win-updt.ps1` (will be deleted anyway)

---

## Phase 3: Formatting Normalization

### Dominant style (per `.editorconfig` and majority files)
- 2-space indent, spaces (no tabs)
- OTBS braces (`function Foo {`)
- CRLF line endings
- UTF-8 with BOM
- No trailing whitespace

### Files to format
| File | Issues |
|------|--------|
| `Scripts/Network-Tweaker.ps1` | Trailing whitespace, Allman braces, commented-out dead code (~lines 2020-2412), 13 consecutive blank lines, missing space before param |
| `Scripts/system-update.ps1` | Trailing whitespace (9 lines) |
| `Scripts/Common.ps1` | Missing UTF-8 BOM |

**Action:** Run `autoformat.py --fix-all` or apply whole-file formatting. For Network-Tweaker.ps1, bulk-format only (no line-by-line verification per user request).

---

## Phase 4: Fix Broken Test References

| Test file | Issue | Fix |
|-----------|-------|-----|
| `tests/Network-Tweaker.Tests.ps1` | Asserts existence of `Opacity`, `Set-ConsoleColor`, `Initialize-AdapterUI` — none exist in target script | Remove invalid assertions or update to actual exported functions |
| `tests/gpu-display-manager.Tests.ps1` | Mocks `Show-NvidiaGpuSettings` (plural) but real function is `Show-NvidiaGpuSetting` (singular) in `Common.ps1:298` | Correct mock name to `Show-NvidiaGpuSetting` |

---

## Phase 5: CI and Validation Fixes

| Issue | Location | Fix |
|-------|----------|-----|
| Test job searches `Scripts/` recursively, but `.Tests.ps1` files live in `tests/` | `.github/workflows/lint-format-test.yml` line 250 | Change search path to `tests/` (and root for `setup.Tests.ps1`) |
| `mise run validate` calls `mise run format` which requires a `file` arg | `mise.toml` line 81 | Remove `mise run format` from validate task, or make format task accept no-arg default (all files) |
| `mise run reg-validate` calls non-existent `Scripts/Validate-Changes.ps1` | `mise.toml` line 85 | Remove task or point to correct script |

---

## Phase 6: Dead Code Removal

| Location | Item | Action |
|----------|------|--------|
| `Scripts/Common.ps1:557` | `function New-QueryString` (never called) | Remove function |
| `.kilo/tools/hashline_edit.ts:2` | Unused `computeHash` import | Remove import |

---

## Phase 7: Validation

1. Run `py-psscriptanalyzer --recursive --severity Warning` (or `mise run lint`) and fix any new issues introduced by moves.
2. Verify all PowerShell files still have UTF-8 BOM and 2-space indent.
3. Run Pester tests locally (`mise run test` or direct `Invoke-Pester`) after fixing test paths.
4. Confirm `Common.ps1` dot-source works in `steam.ps1` and `start-arc-raiders.ps1`.

---

## Assumptions
- `Common.ps1` is already dot-sourced by most scripts; we will ensure it is for `steam.ps1` and `start-arc-raiders.ps1`.
- Deleting orphaned `.github/scripts/*.ps1` is safe because CI duplicates their logic inline.
- Network-Tweaker.ps1 is a large WinForms GUI script; whole-file formatting will not change runtime behavior.

## Summary of Expected Changes
- **~13 files removed** (orphans + superseded scripts)
- **~8 scripts updated** to use shared elevation/directory/VDF helpers
- **Common.ps1 expanded** with `Ensure-Directory`, `vdf_mkdir`, `sc-nonew`
- **3 test files fixed**
- **CI workflow + mise.toml corrected**
- **Bulk formatting** applied to Network-Tweaker.ps1, system-update.ps1, Common.ps1
