# Plan: Validate and Fix Dotfile Bootstrap for Fresh Windows 11 Install

## Goal
Ensure the entire bootstrap and deployment system works correctly when executed as a post-install script on a clean, fresh Windows 11 install.

## Context & Scope
Affected layers:
1. Internet bootstrap (`.github/scripts/bootstrap.ps1`)
2. Repo bootstrap (`install.conf.yaml` + `Scripts/Setup-Dotfiles.ps1`)
3. Unattended USB install (`Scripts/auto/autounattend.xml`)
4. Tracked configs (`user/.dotfiles/config/`)
5. CI workflows (`.github/workflows/`)

## Critical Path Fixes (Must Fix for Fresh Install to Work)

### 1. Fix `Scripts/Setup-Dotfiles.ps1` path resolution bug
- **Issue**: `$configRoot = Join-Path $HOME 'user\.dotfiles\config'` is broken because repo is cloned under `$HOME\Win`, not `$HOME` directly.
- **Fix**: Resolve relative to `$PSScriptRoot` (or dotbot-provided environment): `$configRoot = Join-Path $PSScriptRoot 'user\.dotfiles\config'`
- **Reference**: `Scripts/Setup-Dotfiles.ps1:435`

### 2. Fix `Scripts/Setup-Dotfiles.ps1` winget argument passing bug
- **Issue**: `$scopeArg = '--scope machine'` passed as single string argument, causing winget failure.
- **Fix**: Use array-based splatting or conditional parameter construction.
- **Reference**: `Scripts/Setup-Dotfiles.ps1:386-389`

### 3. Fix autounattend.xml `stage2.ps1` undefined `Wait-ForWinget` function
- **Issue**: `stage2.ps1` calls `Wait-ForWinget`, but it's defined only in `install.ps1` (scope lost after reboot).
- **Fix**: Inline the wait logic into `stage2.ps1`, or define the function there.
- **Reference**: `Scripts/auto/autounattend.xml:1230`

### 4. Fix autounattend.xml global `$ErrorActionPreference = 'Continue'`
- **Issue**: Hides failures from `dism`, `Install-WindowsUpdate`, and winget calls silently.
- **Fix**: Remove global preference; use `-ErrorAction Stop` or explicit handling per cmdlet.
- **Reference**: `Scripts/auto/autounattend.xml:1098`

## High-Priority Fixes (CI / Deployment Completeness)

### 5. Fix broken GitHub workflow syntax errors
- **Files affected**:
  - `.github/workflows/lint-format-test.yml` — multiple missing `}` before `else`, bash `}` instead of `fi`
  - `.github/workflows/ps-format.yml` — corrupt `$env:` variable reference with injected YAML text
  - `.github/workflows/copilot-setup-steps.yml` — stale paths to `.github/instructions/`, missing `./.github/actions/setup-pwsh`, Python 3.14 pre-release

### 6. Add missing tracked configs to deployment manifest
- **Missing from `Setup-Dotfiles.ps1` manifest / `install.conf.yaml`**:
  - `games/arc-raiders/` — AGENTS.md says this changes together with scripts, but completely missing from deployment
  - `DDU/` — Display Driver Uninstaller config
  - `cursors/` — Custom cursor set
  - `mise/` — mise config.toml
  - `msi-afterburner/` — Afterburner skin
  - `nvidia-inspector/` — Inspector settings script
  - `scoop/` — Scoop config
  - `winget-configs/` — Winget settings
- **Note**: Some may be intentionally manual (like `nvidia/`). Verify intent before adding — add comment/documentation for those kept manual.

### 7. Fix wrong URL in `bootstrap.ps1` comment header
- **Issue**: Script comment says `.../main/bootstrap.ps1` instead of `.../main/.github/scripts/bootstrap.ps1`
- **Fix**: Correct the URL reference
- **Reference**: `.github/scripts/bootstrap.ps1:12`

## Medium-Priority Fixes (Robustness & Consistency)

### 8. Remove hardcoded Steam ID from tracked config
- **Issue**: `user/.dotfiles/config/games/bo6/76561198997504391/` — a hardcoded Steam ID directory will be deployed to other machines.
- **Fix**: Move to a template/pattern-based approach, add to `.gitignore`, or document as machine-specific.

### 9. Fix Invoke-Expression usage in PowerShell profile
- **Issue**: `Invoke-Expression` in `profile.ps1` triggers CI-enforced `PSAvoidUsingInvokeExpression`.
- **Fix**: Use `.` sourcing or `Invoke-Command` where possible, or add `# PSScriptAnalyzer` suppression with justification if truly required.
- **Reference**: `user/.dotfiles/config/powershell/profile.ps1:12, 22`

### 10. Ensure `install.conf.yaml` and `Setup-Dotfiles.ps1` are consistent
- **Issue**: `games/bo7/` is in `$configManifest` but missing from `install.conf.yaml`
- **Issue**: `games/arc-raiders/` has separate deployment logic in `Deploy-Config.ps1` but not wired into main flow
- **Fix**: Align both manifests; document any intentional exclusions.

## Validation Block (Run After Fixes)

After all edits:
1. `Invoke-ScriptAnalyzer -Path Scripts/Setup-Dotfiles.ps1 -Settings PSScriptAnalyzerSettings.psd1`
2. `$xml = [xml]::new(); $xml.Load('Scripts/auto/autounattend.xml')`
3. Verify `install.conf.yaml` YAML syntax and path references
4. Run Pester tests: `Invoke-Pester -Path tests/ -Output Minimal`
5. Optional: `npx -y @yawlabs/ctxlint --depth 3 --mcp --strict` for guidance consistency

## Execution Notes
- preserve PowerShell 5.1+/7+ compatibility
- Use `$PSScriptRoot`, `$HOME`, `$env:*` — never hardcoded `C:\Users\...`
- Do not use global `SilentlyContinue`
- Do not use `Invoke-Expression` with untrusted input
- Follow OTBS braces, 2-space indent, full cmdlet names

---
Generated: 2026-05-04
