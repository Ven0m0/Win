[settings]
experimental = true
color = true
yes = true
auto_install = true

[settings.python]
uv_venv_auto = "create|source"
uv_venv_create_args = ["--seed", "--system-site-packages"]

[settings.pipx]
uvx = true

[settings.dotnet]
cli_telemetry_optout = true

[tools]
python = "3.14"
uv = "latest"
ruff = "latest"
ty = "latest"
prek = "latest"
"pipx:pytest" = "latest"
"pipx:basedpyright" = "latest"
"pipx:dotbot" = "latest"
"pipx:py-psscriptanalyzer" = "latest"
#"pipx:pwsh" = "latest"
#"pipx:WMI" = "latest"
#"pipx:psutil" = "latest"

[env]
PROJECT_ROOT = "{{config_root}}"
# Ensure uv uses the exact Python interpreter installed by `mise install`.
# `tools = true` makes `tools.python.path` available when mise resolves tool-aware env vars at runtime.
UV_PYTHON = { value = "{{ tools.python.path }}", tools = true }
UV_LINK_MODE = "symlink"
_.python.venv = { path = ".venv", create = true, uv_create_args = ["--seed", "--system-site-packages"] }

[tasks.bootstrap]
description = "Install dotbot and deploy dotfiles"
run = ["pip install dotbot", "dotbot -c install.conf.yaml"]

[tasks.deploy]
description = "Deploy dotfiles (dotbot must be installed)"
run = "dotbot -c install.conf.yaml"

[task.dev]
description = "Install powershell lint+format utils"
run = ["curl -sSfL https://webi.sh/pwsh-essentials | bash && source ~/.config/envman/PATH.env"]

[task.pwsh-install]
description = "Install pwsh lint"
run = ["pwsh -NoLogo -NoProfile -Command '
Set-PSRepository PSGallery -InstallationPolicy Trusted; Install-Module PSScriptAnalyzer -Scope CurrentUser -Force -AllowClobber'"]
