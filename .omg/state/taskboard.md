# Taskboard

## In Progress
- None

## Done
- [x] Modify steam.ps1 to remove `#Requires -RunAsAdministrator`.
- [x] Update registry keys for "Copy Path" (`*` and `Directory`) to execute PowerShell silently (`-WindowStyle Hidden`).
- [x] Fix escaping format to strip leading/trailing quotes and specifically `TrimStart('\')` to remove the leading backslash reported by the user.